import React from"react";import{CheckCircle2,ExternalLink}from"lucide-react";export default function SourceCard({source}){return <a className="source-card" href={source.url} target="_blank" rel="noreferrer"><div className="source-icon"><CheckCircle2 size={18}/></div><div className="source-content"><span className="source-status">APPROVED SOURCE</span><strong>{source.title||"Jain AI Knowledge Source"}</strong>{typeof source.similarity==="number"&&<small>{Math.round(source.similarity*100)}% semantic match</small>}</div><ExternalLink size={16}/></a>}
