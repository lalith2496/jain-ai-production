import React from"react";import{Zap,BrainCircuit,BookOpen,GraduationCap}from"lucide-react";const modes=[["quick","Quick",Zap],["deep","Deep dive",BrainCircuit],["story","Story mode",BookOpen],["study","Study",GraduationCap]];export default function LearningModes({activeMode,onChange}){return <div className="learning-modes">{modes.map(([id,label,Icon])=><button key={id} className={activeMode===id?"active":""} onClick={()=>onChange(id)}><Icon size={16}/>{label}</button>)}</div>}
